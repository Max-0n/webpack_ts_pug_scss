include ./mixins.pug
doctype html
html
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    title TS, PUG, SCSS
  body
    .wrapper
      +build-with-love()
      +made-with-pug()

    .wrapper
      +gluten-free()
      +makes-people-smile()
      +contains-cat-gifs()

    .wrapper
      +uses-badges()
      +uses-css()
      +uses-html()
      +uses-js()

    .wrapper
      +validated-html5()
      +you-didnt-ask-for-this()
